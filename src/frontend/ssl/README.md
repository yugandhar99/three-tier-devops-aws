# Local TLS Certificates

Local `.pem` certificates are intentionally not committed to GitHub. If HTTPS is needed for local testing, generate local development certificates and place them in this folder.

Production TLS is handled through AWS ACM and CloudFront/ALB in Terraform.
